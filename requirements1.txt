# 웹 프레임워크
flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0

# NumPy 1.x 고정 (BasicSR 호환성을 위해 필수)
numpy==1.24.3

# 이미지 처리
pillow==10.2.0
opencv-python-headless==4.8.1.78

# PyTorch (CPU 버전 - 메모리 절약)
torch==2.0.1+cpu
torchvision==0.15.2+cpu
--find-links https://download.pytorch.org/whl/torch_stable.html

# BasicSR (핵심 패키지)
basicsr==1.4.2

# BasicSR 의존성들 (호환 버전으로 고정)
scipy==1.10.1
scikit-image==0.21.0
imageio==2.31.1
tqdm==4.65.0
pyyaml==6.0
addict==2.4.0
lmdb==1.4.1

# 기타 필요한 패키지들
requests==2.31.0
tb-nightly==2.15.0a20230916
